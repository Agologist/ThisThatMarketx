export async function bridgeEthToBase(amountInEth: number) {
  // This is where you'd use Orbiter, Across, or Socket bridge SDK
  console.log(`💸 Bridge ${amountInEth} ETH from Polygon → Base`);
  // placeholder logic
}